---
title: Architecture Overview
description: AnvilKit engine architecture design
---

## Layered Architecture

AnvilKit uses a layered modular design where each layer depends only on layers below it:

<Mermaid chart={`
flowchart TB
  subgraph game["🎮 Game Layer"]
    billiards["billiards"]
    craft["craft"]
    your_game["your_game"]
  end
  subgraph system["📦 System Layer"]
    umbrella["anvilkit (umbrella)"]
    app["anvilkit-app"]
    camera["anvilkit-camera"]
    audio["anvilkit-audio"]
    input["anvilkit-input"]
    assets["anvilkit-assets"]
    ui["anvilkit-ui"]
    gameplay["anvilkit-gameplay"]
    data["anvilkit-data"]
  end
  subgraph core["⚙️ Core Layer"]
    ecs["anvilkit-ecs"]
    render["anvilkit-render"]
    corelib["anvilkit-core"]
  end
  subgraph deps["🔧 Third-party"]
    bevy["bevy_ecs"]
    wgpu["wgpu"]
    rapier["rapier3d"]
    glam["glam"]
    taffy["taffy"]
  end
  game --> system --> core --> deps
`} />

## Crate Dependency Graph

<Mermaid chart={`
flowchart BT
  core["anvilkit-core\n(math, time, error, persistence)"]
  ecs["anvilkit-ecs\n(App, Plugin, Schedule, Transform, Physics)"]
  render["anvilkit-render"]
  input["anvilkit-input\n(InputState, ActionMap)"]
  camera["anvilkit-camera\n(5 modes, trauma shake, spring arm, rail)"]
  assets["anvilkit-assets\n(glTF, animation, AssetServer)"]
  audio["anvilkit-audio\n(AudioSource, spatial)"]
  app["anvilkit-app\n(AnvilKitApp, GameCallbacks, GameConfig)"]
  ui["anvilkit-ui\n(Widget, flexbox layout, UiPlugin)"]
  gameplay["anvilkit-gameplay\n(Health, Inventory, Cooldown, StatusEffect)"]
  data["anvilkit-data\n(DataTable, Locale)"]

  core --> ecs
  ecs --> render
  ecs --> input
  render --> camera
  input --> camera
  core --> assets
  ecs --> audio
  ecs --> app
  render --> app
  input --> app
  core --> app
  ecs --> gameplay
  ecs --> data
  ecs --> ui
`} />

## Per-Frame Lifecycle

<Mermaid chart={`
flowchart TD
  subgraph EL["Event Loop (winit)"]
    direction TB

    subgraph E0["0. resumed (once)"]
      gcinit["GameCallbacks::init()"]
    end

    subgraph E1["1. window_event / device_event"]
      fwd_input["forward_input() → InputState"]
      fwd_device["forward_device_input() → mouse_delta"]
    end

    subgraph E2["2. about_to_wait → tick()"]
      dt["DeltaTime = elapsed()"]
      subgraph update["app.update()"]
        direction TB
        evflush["Events flush"]
        main["Main"]
        pre["PreUpdate"]
        fixed["FixedUpdate (0-N ticks)"]
        upd["Update"]
        post["PostUpdate"]
        cleanup["Cleanup"]
        evflush --> main --> pre --> fixed --> upd --> post --> cleanup
      end
      gcpost["GameCallbacks::post_update()"]
      endframe["InputState.end_frame()"]
      redraw["window.request_redraw()"]
      dt --> update --> gcpost --> endframe --> redraw
    end

    subgraph E3["3. RedrawRequested → GameCallbacks::render()"]
      shadow["Shadow Pass ×3 (CSM)"]
      hdr["HDR Scene Pass (PBR + MSAA 4×)"]
      ssao["SSAO Pass"]
      dof["DOF Pass"]
      mblur["Motion Blur Pass"]
      bloom["Bloom Pass"]
      cg["Color Grading Pass"]
      tone["Tonemap Pass"]
      hud["HUD Pass"]
      shadow --> hdr --> ssao --> dof --> mblur --> bloom --> cg --> tone --> hud
    end

    E0 -.-> E1 --> E2 --> E3
  end
`} />

## ECS Schedule Phases

| Phase | Timing | Typical Systems |
|-------|--------|-----------------|
| `Startup` | Once at init | Resource setup, initial spawn |
| `Main` | Every frame, first | (reserved for game-wide logic) |
| `PreUpdate` | Every frame, before Update | DeltaTime sync, parent-child hierarchy sync |
| `FixedUpdate` | 0-N per frame (accumulator) | Deterministic physics, fixed-rate gameplay |
| `Update` | Every frame, main logic | Input, physics, game logic, camera |
| `PostUpdate` | Every frame, after Update | Transform propagation, render extract |
| `Cleanup` | Every frame, last | (reserved) |

## Feature Flags

| Crate | Feature | What it enables |
|-------|---------|-----------------|
| `anvilkit-core` | `serde` | Serialize/Deserialize for Transform, GlobalTransform |
| `anvilkit-core` | `persistence` | SaveManager, Settings, WorldStorage (RON + file KV) |
| `anvilkit-core` | `bevy_ecs` | Component derives for math types |
| `anvilkit-ecs` | `serde` | Serialize for Parent, Children, scene serialization |
| `anvilkit-ecs` | `rapier` | rapier3d physics runtime integration |
| `anvilkit-assets` | `hot-reload` | File watching via `notify` crate |
| `anvilkit-assets` | `bevy_ecs` | Component derives for Skeleton, AnimationPlayer |
| `anvilkit-gameplay` | `stats` | Stats, Health, DamageEvent, health_system |
| `anvilkit-gameplay` | `inventory` | SlotInventory, StackInventory, ItemStack |
| `anvilkit-gameplay` | `cooldown` | Cooldown, cooldown_tick_system |
| `anvilkit-gameplay` | `status-effect` | StatusEffect, StatusEffectList, StackPolicy |
| `anvilkit-gameplay` | `entity-pool` | EntityPool for entity recycling |

## Module Overview

### anvilkit-core

Foundation type library: math utilities (Vec3, Mat4, Transform, GlobalTransform, interpolation), time system (Time, Timer), error types (AnvilKitError with category-preserving `with_context`), persistence system (SaveManager, Settings, WorldStorage — all returning `Result<_, AnvilKitError>`). Aabb axis-aligned bounding box, Frustum, and raycast utilities (screen_to_ray, ray_plane_intersection, ray_sphere_intersection) — moved from render crate in v0.3. Persistence error category (AnvilKitError::Persistence) with category-preserving with_context.

### anvilkit-ecs

Application framework based on `bevy_ecs 0.14`: App lifecycle (new → add_plugins → add_systems → run), Plugin system with uniqueness checks, Schedule dispatching (Main/Startup/PreUpdate/Update/PostUpdate/Cleanup), AppExit resource for graceful shutdown, Transform hierarchy (Parent/Children/GlobalTransform propagation with auto-sync), Physics (scale-aware AABB collision + rapier3d integration), Scene serialization (RON), FixedUpdate schedule (accumulator-based deterministic timestep), Events system (bevy_ecs `Events<T>`/EventWriter/EventReader), `GameState<S>` state machine, SerializableRegistry for type-erased scene serialization, AutoInputPlugin and AutoDeltaTimePlugin for automatic frame management. PersistencePlugin for auto-save integration, SettingsApplyPlugin for syncing Settings to engine subsystems. Physics split into module directory: components.rs, aabb.rs, rapier.rs, events.rs.

### anvilkit-render

Unified 2D/3D rendering pipeline:
- PBR material system (Cook-Torrance BRDF, 5 texture maps)
- HDR rendering (Rgba16Float) + ACES Filmic tone mapping
- Bloom (13-tap downsample + 9-tap tent upsample, 5 mip levels)
- SSAO (hemisphere sampling, half-res, depth-reconstructed normals)
- Cascade Shadow Maps (3 cascades, D2Array, PCF)
- IBL (BRDF LUT + environment irradiance)
- MSAA 4× anti-aliasing
- GPU skeletal animation (Linear Blend Skinning, 128 joints)
- Frustum culling + GPU instancing + batched draw sorting
- Sprite, Line, Particle, Text, UI renderers
- StandardMaterial component (PBR with builder pattern)
- PostProcessSettings (SSAO/DOF/MotionBlur/Bloom/ColorGrading)
- Orthographic/Perspective projection enum, multi-camera priority
- PipelineCache/BindGroupCache
- Point/spot shadow system (ShadowAtlas)
- events.rs split into 6 files (events/ directory), draw.rs split into 5 files (draw/ directory)
- CachedBuffer shared utility replacing 5 duplicate cached_vb patterns
- ProjectionUniform shared type replacing 5 duplicate ortho structs
- DebugRenderer merged with LineRenderer

### anvilkit-assets

Asset pipeline: glTF model loading (mesh, material, skeleton, animation), AssetServer (sync + async loading, deduplication), AssetHandle (Arc refcount), procedural mesh generation, file watching hot reload. DependencyGraph for asset dependency tracking (AssetId-based). AssetCache integration (content hash deduplication). Cascade unload through dependency graph. insert_parsed/get_parsed for typed asset storage.

### anvilkit-audio

Audio system based on `rodio`: AudioSource component with ECS integration, AudioBus (master/music/sfx/voice volume mixing), spatial audio (distance attenuation via AudioListener), looping and pitch control. NonSend resource pattern for thread-safe macOS CoreAudio. Stereo panning via listener right vector. Asset integration via AudioSource.asset_id.

### anvilkit-input

Input abstraction: InputState (keyboard/mouse pressed/just_pressed/released, mouse_delta, scroll_delta), GamepadState (16 buttons, 6 axes), ActionMap (logical action → physical binding), ActionId (zero-allocation lookups), AxisBinding (keyboard/gamepad analog axes). InputPlugin for automatic resource initialization. ActionMap with apply_overrides for key rebinding. Gamepad event forwarding.

### anvilkit-camera

Production camera system with 5 modes (FirstPerson, ThirdPerson, Orbit, Free, Rail). Features: OrbitState component, CameraRig entity follow, SpringArm collision avoidance, trauma-based Perlin noise shake, CameraTransition blending (5 easing curves), LookAtTarget soft constraint, CameraRail dolly (Catmull-Rom), InputCurve dead zone + response curves. 8-system pipeline, frame-rate independent smoothing.

### anvilkit-app

Game application runner that eliminates boilerplate. `AnvilKitApp::run()` owns the winit event loop, input forwarding, DeltaTime management, and frame lifecycle. Games implement `GameCallbacks` (init, post_update, render, on_resize, on_window_event) and pass a `GameConfig` (title, size, vsync). Provides `GameContext` for access to the ECS `App` and `RenderApp`.

### anvilkit-ui

Retained-mode UI framework with flexbox layout (taffy-based). GPU-independent — rendering is delegated to `anvilkit-render`'s `UiRenderer`. Includes `Widget` (button, label, panel), `UiLayoutEngine`, event system (hit test, interactions), `UiTheme`, focus management, `UiPlugin` for ECS integration, and interactive controls (TextInput, Slider, Checkbox, Dropdown, ScrollView).

### anvilkit-gameplay

Optional, feature-gated gameplay building blocks: `stats` (generic stat system with additive/multiplicative/override modifiers), `health` (Health component with regen, DamageEvent, HealEvent, DeathEvent, health_system), `inventory` (slot-based and stackable items), `cooldown` (ability/action cooldown timers), `status-effect` (duration-based effects with stacking policies), `entity-pool` (object pool for entity recycling).

### anvilkit-data

Data-driven configuration and localization: `DataTable` (typed key-value table loaded from RON or JSON), `Locale` (translation lookup with fallback chain), `DataTablePlugin` for ECS integration.

## Coordinate System

AnvilKit uses a **left-handed coordinate system**:

- +X points right
- +Y points up
- +Z points into the screen (forward)

Projection uses `perspective_lh` and `look_at_lh`.
